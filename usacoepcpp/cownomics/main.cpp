#include <iostream>
#include <fstream>
using namespace std;
int main() {
    ifstream fin ("cownomics.in");
    ofstream fout ("cownomics.out");
    int N;
    int M;
    string SG[                             ]
    string A[N], B[N];
    fin >> N >> M;
    for (int i = 0; i < N; i++) fin >> A[i] >> B[i];
    for(int i = 0; i < M; i++) {

    }

    return 0;
}